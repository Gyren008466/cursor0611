name = "cursor0611"
compatibility_date = "2024-09-01"
pages_build_output_dir = "dist"

# Cloudflare Pages Git 部署（Dashboard → Settings → Build）：
#   Build command:  npm run build
#   Output directory: dist
#   Deploy command:  留空（切勿填 npx wrangler deploy）
# 环境变量在 Dashboard → Settings → Environment variables 配置
